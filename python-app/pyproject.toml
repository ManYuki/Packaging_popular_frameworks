[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "python-packaging-demo"
version = "1.0.0"
description = "Django application for packaging demonstration"
dependencies = [
    "Django==4.2.5",
    "django-environ==0.11.2"
]
